version: '3.8'

services:
  biblioteca:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: biblioteca-app
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - biblioteca-network

networks:
  biblioteca-network:
    driver: bridge
